# ################ #
# Emerald Settings #
# ################ #

# Configure the connection to redis.
redis:
  host: "localhost"
  port: 6379
  auth:
    enabled: true
    password: "emerald"

server:
  name: "Lobby"
  group: "HUB"
  groups: # Make sure these values match in all servers.
    - "EU"
    - "NA"
    - "HUB"
  uuid: null # Do not edit this.